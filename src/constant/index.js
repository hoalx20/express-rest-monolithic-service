const failure = require('./failure');
const success = require('./success');
const global = require('./global');
const permission = require('./permission');

module.exports = { failure, success, global, permission };
